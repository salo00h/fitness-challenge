{
  "name": "fitness-challenge-admin-scripts",
  "private": true,
  "version": "1.0.0",
  "description": "سكربتات إدارية تعمل بـ Firebase Admin SDK - لا تُشحن أبدًا للواجهة الأمامية.",
  "type": "commonjs",
  "scripts": {
    "migrate:dry-run": "node migrate-users-to-auth.js --dry-run",
    "migrate": "node migrate-users-to-auth.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
