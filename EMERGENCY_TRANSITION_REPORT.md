# تقرير تنفيذ طارئ — نشر قواعد انتقالية آمنة

**التاريخ:** 2026-07-19
**الحساب المستخدم:** saleh738297@gmail.com (عبر Firebase CLI بعد `firebase login` الذي نفّذته أنتِ)
**المشروع:** `fitness-challenge-6061e` (الوحيد المتاح على هذا الحساب - تم التأكد قبل أي نشر)

## 1) النسخة الاحتياطية

- **الوقت:** 2026-07-19 11:41 UTC تقريبًا
- **المكان:** `backups/firebase-emergency-2026-07-19-11-41/`
- **العدد الفعلي للوثائق وقت الأخذ:** `participants`=2، `exercises`=44 (42 تمرين + 2 بيانات تحدٍ)، `notifications`=155، `game-state`=2، (`public-profiles`/`legacy-participants`/`roles` = 0 لكل منها، لم تُستخدم بعد).
- تفاصيل صفاء وسحر الكاملة (لكل أسبوع في كل تحدٍ) موجودة في `backup-report.md` و`per-participant-analysis.txt` داخل نفس المجلد.

## 2) اكتشاف مهم أثناء الفحص

كل تعديلات الجلسة السابقة (Firebase Auth الكامل) كانت لا تزال **غير منشورة وغير مُلتزَمة بـ git** — لم تصل لأي مستخدمة حقيقية. التطبيق الذي تستخدمه صفاء وسحر فعليًا ظل طوال الوقت يعمل بالنظام القديم (بدون Firebase Auth)، لذلك لم يحدث أي توقف فعلي سابقًا بسبب ذلك.

كذلك تحققت من `firebase hosting:sites:list` — يوجد موقع Firebase Hosting باسم المشروع (`fitness-challenge-6061e.web.app`) لكنه **"Site Not Found"** (لم يُنشر عليه أي شيء إطلاقًا). أي أن التطبيق الحي الذي تستخدمانه مستضاف من مكان آخر لا أعرفه (خادم شخصي، مجلد مشترك، أو استضافة أخرى) — لذلك لم أستطع فتحه مباشرة في متصفح لأختبره كصفحة حقيقية. الاختبار البديل الذي نفّذته أدناه (طلبات Firestore مباشرة بنفس الأنماط والمسارات التي يستخدمها الكود القديم فعليًا) هو أدق اختبار ممكن متاح لي بدون معرفة عنوان الموقع الحقيقي.

## 3) نشر القواعد الانتقالية

```
firebase use fitness-challenge-6061e   → Exit Code 0
firebase deploy --only firestore:rules → Exit Code 0
```

الناتج الفعلي: `+ firestore: released rules firestore.rules to cloud.firestore` ثم `+ Deploy complete!`. **تم النشر فعليًا وليس افتراضًا.**

القواعد المنشورة (`firestore.rules` في جذر المشروع) متوافقة تمامًا مع الكود القديم الحي (بدون Auth)، وتضيف فقط:
- منع حذف `participants` و`game-state` نهائيًا.
- منع حذف كل `exercises` دفعة واحدة.
- منع أي وصول لـ Collections غير معروفة (بما فيها `roles`/`public-profiles`/`legacy-participants` المخصصة للمرحلة القادمة).
- تحقق أساسي أن أي وثيقة participants تحمل حقل `name` نصيًا.
- صلاحية الكتابة تنتهي تلقائيًا في **2026-08-02** كشبكة أمان إضافية (القراءة تبقى مفتوحة دائمًا).

## 4) اختبار فعلي بعد النشر (وليس نظريًا)

نُفِّذ عبر استدعاءات Firestore مباشرة (نفس الدوال التي يستخدمها `js/*.js` القديم فعليًا)، ضد قاعدة البيانات الحقيقية بعد النشر:

| الاختبار | النتيجة |
|---|---|
| قراءة `participants` الحقيقية (صفاء + 🌸Sahar) | ✅ نجح، البيانات سليمة بالكامل (23 و27 done على التوالي) |
| كتابة/تعديل وثيقة participant تجريبية (بحقل name) | ✅ نجح (يثبت أن حفظ التقدم اليومي سيستمر يعمل) |
| حذف وثيقة participant | ✅ رُفض (`permission-denied`) كما هو مقصود |
| كتابة تمرين تجريبي في `exercises` | ✅ نجح (admin.html يستمر بالعمل) |
| حذف تمرين | ✅ رُفض كما هو مقصود |
| كتابة `game-state` تجريبية | ✅ نجح |
| حذف `game-state` | ✅ رُفض كما هو مقصود |
| إنشاء وحذف `notifications` تجريبية | ✅ نجح (رسائل التطبيق غير متأثرة) |
| كتابة في Collection غير معروفة | ✅ رُفض (`permission-denied`) |
| قراءة `roles` | ✅ رُفض (`permission-denied`) |
| كتابة participant بدون حقل `name` | ✅ رُفض (`permission-denied`) - التحقق من الشكل يعمل |

كل الوثائق التجريبية التي أنشأتها لهذا الاختبار **حُذفت فورًا بعد ذلك عبر Firebase CLI** (صلاحية مالك تتجاوز القواعد، غير متاحة للعميل العادي) - تم التأكد أن `participants`=2 (صفاء + سحر فقط) و`exercises`=44 و`game-state`=2 عادت لعددها الأصلي تمامًا بعد التنظيف. **لا أثر لأي بيانات تجريبية متبقية.**

## 5) الخلاصة

- ✅ لا فقدان بيانات - صفاء وسحر وكل تقدم التحدي الأول والثاني سليم تمامًا.
- ✅ التطبيق الحالي (بدون Auth) يستمر بالعمل كما هو تمامًا تحت القواعد الجديدة.
- ✅ القواعد الآن أفضل من Test Mode المفتوح: تمنع الحذف الخطير وتمنع أي استخدام لمسارات غير معروفة.
- ⚠️ القيد المتبقي (موثّق بصراحة): بدون Firebase Auth حقيقية، لا يمكن لأي قاعدة منع مشاركة من تعديل بيانات مشاركة أخرى بمعرفة اسمها فقط - وهذا لم يتغير عن وضعه السابق (لا تراجع، لكن لا تقدم أمني في هذه النقطة تحديدًا حتى تكتمل هجرة Auth).
- 🕐 المهلة: 14 يومًا (حتى 2026-08-02) قبل أن تتوقف الكتابة تلقائيًا - يجب إكمال الانتقال لـ Firebase Auth قبل هذا التاريخ.

**لم أنتقل إلى القواعد النهائية أو تفعيل Firebase Auth بعد، بناءً على طلبك الصريح بالتوقف بعد نجاح هذه الخطوات.**
